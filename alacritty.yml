# Configuration for Alacritty, the GPU enhanced terminal emulator. For config options, look at the example github config

# Import additional configuration files
#
# Imports are loaded in order, skipping all missing files, with the importing
# file being loaded last. If a field is already present in a previous import, it
# will be replaced.
#
# All imports must either be absolute paths starting with `/`, or paths relative
# to the user's home directory starting with `~/`.
import:
  - ~/.config/alacritty/catppuccin-mocha.yml
env:
  TERM: xterm-256color
window:
  padding:
    x: 5
    y: 5
  decorations: buttonless
  opacity: 0.95
  startup_mode: Maximized
font:
  normal:
    family: MesloLGSDZ Nerd Font
    style: Regular
  bold:
    family: MesloLGSDZ Nerd Font
    style: Bold
  italic:
    family: MesloLGSDZ Nerd Font
    style: Italic
  bold_italic:
    family: MesloLGSDZ Nerd Font
    style: Bold Italic
  size: 15.0
cursor:
  vi_mode_style:
    shape: Block
    blinking: Off
live_config_reload: true
alt_send_esc: false # On Macos alt-KEY's are overridden if you set this to true "[]\{}@..."
